// Stub for expo/src/winter/* modules.
// These files use import.meta syntax which Jest (CommonJS) does not support.
// Returning an empty object is safe — these modules only set up Metro-specific
// runtime globals that are not needed in the Jest environment.
module.exports = {};
